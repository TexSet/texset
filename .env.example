# Port the app listens on inside the container
PORT=7474

# Where project files are stored (maps to the bind mount)
TEXSET_PROJECTS_DIR=/app/projects

# Linux file ownership. On macOS and Windows (with Docker Desktop),
# you can leave these at the defaults -- file permissions are handled
# by the VM layer automatically.
UID=1000
GID=1000
